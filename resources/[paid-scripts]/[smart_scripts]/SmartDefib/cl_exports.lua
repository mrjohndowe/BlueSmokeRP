function blipsEnable(boolean)
    enableBlips(boolean)
end

function playerRevived()
    -- This function is called when the ped is revived locally, allowing you to trigger an event or show a notification
end